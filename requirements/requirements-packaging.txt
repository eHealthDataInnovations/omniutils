# Configurações de desenvolvimento
## Permite carregar variáveis de ambiente a partir de arquivos .env
python-dotenv>=1.0.1

# Biblioteca mais popular para análise e manipulação de dados em Python,
# permitindo trabalhar com DataFrames e Séries.
pandas>=2.2.3

# Biblioteca fundamental para computação numérica em Python, permitindo
# manipulação eficiente de arrays e operações matemáticas avançadas.
numpy>=2.2.2

# Usada para extração e parsing de dados HTML e XML, facilitando o web scraping.
beautifulsoup4>=4.13.3

# Biblioteca para requisições HTTP em Python, facilitando a comunicação com
# APIs.
requests>=2.32.3

# Biblioteca para cache de requisições HTTP, melhorando a performance de
# aplicações que fazem muitas requisições.
requests-cache>=1.2.1 # Cache requests

# Tenta identificar a data de publicação ou modificação de uma página HTML. Ela
# analisa metadados (como meta tags, microformats e outros padrões embutidos)
# para extrair e retornar a data associada ao conteúdo da página, sendo útil
# para aplicações que precisam organizar ou filtrar conteúdos com base em sua
# data.
htmldate>=1.9.3

# Cache persistente e baseada em disco para Python. Ela oferece uma interface
# semelhante a um dicionário para armazenar objetos, mas os dados são salvos no
# disco, permitindo que o cache persista entre as execuções do programa.
diskcache>=5.6.3

# ler, escrever e modificar arquivos do Excel no formato .xlsx (e variantes,
# como .xlsm e .xltx).
openpyxl>=3.1.5

# Focada na leitura de arquivos do Excel, especialmente aqueles no formato
# legado .xls (Excel 97-2003)
xlrd>=2.0.1

# É um visualizador e ferramenta leve para PDFs, XPS e outros formatos de
# documentos. Permite extrair texto, renderizar páginas, manipular anotações e
# realizar diversas operações em documentos PDF com alta performance.
PyMuPDF>=1.24.11

# Converte texto Unicode em uma versão ASCII aproximada. Ela remove acentuação
# e caracteres especiais, facilitando a normalização de textos para uso em URLs,
# nomes de arquivos ou para comparações sem considerar variações de acentuação
unidecode>=1.3.8
